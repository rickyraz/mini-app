---
import Layout from "../layouts/Layout.astro";
import Box from "../components/Box.astro";
const items = [
  {
    alt: "Calcultor app",
    image: "/calc.webp",
    href: "./collections/calculator",
  },
  {
    alt: "Countdown app",
    image: "/count.webp",
    href: "./collections/countdown",
  },
];
---

<Layout title="rickyrazhari - Mini App" bgcolor="bg-[#ffffff]">
  <main>
    <h1
      class="font-bold font-poppins flex justify-center text-2xl pt-12 text-[#081127]"
    >
      Mini App Collections
    </h1>
    <ul
      class="grid sm:grid-cols-2 xl:grid-cols-3 gap-10 md:gap-14 xl:gap-20 px-10 md:px-32 pt-12"
    >
      {
        items.map((item) => (
          <Box alt={item.alt} src={item.image} href={item.href} />
        ))
      }
    </ul>
  </main>
</Layout>
